{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./example/socket/http/httpserver/httpserver",
            "cwd": "${workspaceRoot}"
        }
    ]
}